jero 기획·설계 단계를 시작합니다.

1. `.claude/agents/jero-planning-design.md` 하네스를 로드하고 모든 규칙을 숙지하세요.
2. `CLAUDE.md` 와 `docs/spec/`(기능명세서·와이어프레임), `docs/design/prototype/`(확정된 화면 = 출발점)을 먼저 정독하세요. 디자인은 고정이며 변경하지 않습니다.
3. `git status` 로 워킹 트리를 확인하세요(클린이 아니면 사용자에게 보고).
4. **기획문서**(`docs/planning/`)를 페이지(화면) 단위로 작성하세요. 각 페이지는 **먼저 대응 `.dc.html` 화면을 확인하고 거기서 기능을 도출**한 뒤, 구성요소를 시안에 매핑하고 **수용 기준(done 조건 체크리스트 + 비기능 한 줄)**을 포함하세요. → **"기획 승인"** 게이트 대기.
5. 승인 후 **설계문서**(`docs/architecture/`)를 **2개 산출물**로 작성하세요(api-spec/backend-design/마이그레이션 문서는 만들지 않음): ① `데이터모델_계약.md`(전 페이지 공유·단일 출처: Supabase 스키마·RLS·생성타입·응답예시), ② `<페이지|묶음>_frontend.md`(컴포넌트 트리·상태경계·쿼리매핑·인터랙션·권한매트릭스·UI카피·폴더네이밍). → **"설계 승인"** 게이트 대기.
6. 코드는 절대 작성하지 마세요(`docs/` 만 수정). 잘못된 방향이면 즉시 중단·보고하세요.

요청: $ARGUMENTS
