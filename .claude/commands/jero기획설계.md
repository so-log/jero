jero 기획·설계 단계를 시작합니다.

1. `.claude/agents/jero-planning-design.md` 하네스를 로드하고 모든 규칙을 숙지하세요.
2. `CLAUDE.md` 와 `docs/spec/`(기능명세서·와이어프레임), `docs/design/prototype/`(시안)을 먼저 정독하세요.
3. `git status` 로 워킹 트리를 확인하세요(클린이 아니면 사용자에게 보고).
4. **기획문서**(`docs/planning/`)를 페이지(화면) 단위로 작성 → **"기획 승인"** 게이트 대기.
5. 승인 후 **설계문서**(`docs/architecture/`)를 작성 → **"설계 승인"** 게이트 대기.
6. 코드는 절대 작성하지 마세요(`docs/` 만 수정). 잘못된 방향이면 즉시 중단·보고하세요.

요청: $ARGUMENTS
