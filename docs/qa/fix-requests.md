# jero — QA 수정사항 트래킹

> 테스트/피드백에서 나온 요청을 모으는 **살아있는 문서**. 항목이 생기면 계속 추가한다.
> 각 항목: 현상 / 원하는 것 / 추정 원인·관련 파일 / 규모 / 상태.
> 상태: ⬜ 미착수 · 🟡 진행 · ✅ 완료

## 우선순위 · 상태 한눈에

| # | 항목 | 구분 | 우선 | 상태 |
|---|---|---|---|---|
| R1 | 모바일 반응형 최적화 | 개선 | 🟠 중 | 🟡 진행 |
| B1 | 초대링크 복사 클립보드 안 됨 | 버그 | 🔴 높음 | ✅ `b548181` |
| B2 | 선택한 나라/도시 무시하고 "도쿄"로 저장 | 버그 | 🔴 높음 | ✅ `a1743ab` |
| B3 | 여행 날짜(기간) 생성 후 수정 불가 | 기능누락 | 🟠 중 | ✅ |
| F1 | Step1 필드 순서 재배치(제목→나라→지역→아이콘→커버) | 개선 | 🟠 중 | ✅ `a1743ab` |
| F2 | 나라·지역/도시 select(콤보박스)로 | 개선 | 🟠 중 | ✅ `a1743ab` |
| F3 | 커버색 커스텀 팔레트(직접 선택) | 개선 | 🟢 낮 | ✅ `aabdacd` |
| D1 | 달력 월 이동 버튼 없음 | 버그/개선 | 🟠 중 | ✅ `a029cbb` |
| D2 | 날짜 input 창 직접 선택·입력 | 개선 | 🟢 낮 | ✅ `a029cbb` |
| D3 | 시작일/종료일 배경색 통일 | 개선(디자인) | 🟢 낮 | ✅ `a029cbb` |
| B4 | "목록" 버튼이 여행 목록으로 이동 안 됨 | 버그 | 🔴 높음 | ✅ `b548181` |
| B5 | 플랜↔캘린더 선택 날짜 미동기화 | 버그 | 🟠 중 | ✅ `a5e8487` |
| U1 | 초대 링크 공유를 멤버 목록 최상위로 | 개선(배치) | 🟢 낮 | ✅ `9f1556c` |
| U2 | "전체 폴더" 라벨/의미 명확화 | 개선 | 🟢 낮 | ✅ `9f1556c`+ |
| U3 | 카테고리 필터 칩 가로 스크롤 → 드롭다운 | 개선 | 🟠 중 | ✅ `9f1556c`+ |
| U5 | 플랜 CategoryBar "전체 폴더" 버튼 vestigial(죽은 버튼) | 버그/정리 | 🟢 낮 | ✅ `e9255c3` |
| U4 | "일정표" 탭 → "캘린더"로 리네이밍 | 개선(카피) | 🟢 낮 | ✅ `9f1556c` |
| B6 | 플랜 Day별 "장소 추가"가 그 날짜에 배정 안 됨 | 버그/UX | 🟠 중 | ✅ `a5e8487` |
| B7 | 마법사 e2e 회귀(flows·template) — 프리필 제거(G) 후 제목 미입력 실패 | 테스트 | 🟠 중 | ⬜ |

---

## R. 반응형 (전 화면 공통) — 진행 중

### R1. 모바일 최적화
- **현상**: 모바일에서 레이아웃이 최적화 안 됨(생성 마법사 등).
- **원하는 것**: 주요 화면(로그인·목록·생성 마법사·워크스페이스 4뷰·팜플렛)을 모바일 폭에서 정상 사용. 데스크톱 1280 우선 + 태블릿/모바일 브레이크포인트.
- **접근(분할 진행 권장)**:
  1. **기반 + 진입 퍼널** — ✅ 완료(2026-07-13, `66ad1d4`): viewport 메타, 브레이크포인트 전략, AppHeader·AuthPanel·TripsHome 모바일 대응(카드 1→2→3열, 탭타깃 44px+). 3뷰포트(375/768/1280) 오버플로 없음·데스크톱 회귀 없음, 144 tests.
  2. **생성 마법사**(모달 풀스크린화) — ✅ 완료(2026-07-13, `9a1cc85`): 구조(F1·F2·B2·D1~D3) 선행 후 반응형. 모바일 풀스크린·스테퍼 축약·필드/날짜 스택·달력/콤보박스 정상, 3뷰포트·데스크톱 회귀 없음, 157 tests.
  3. **워크스페이스** (시안: `docs/design/prototype/모바일 화면.dc.html`):
     - 3-A 상단 바 + 탭 드로어(햄버거→왼쪽 드로어 5탭) — ✅ 완료(`fef83e9`, `useWorkspaceView` 단일 출처, 176 tests)
     - 3-B 플랜(Day 스위처 + [리스트|지도] 세그먼트 토글) — ✅ 완료(`ec34302`, MapCanvas ResizeObserver로 회색타일 방지, 180 tests)
     - 3-C 캘린더(7일 스트립 + 선택일 아젠다) — ✅ 완료(`925e0a0`, 여행 첫날 기준 주 앵커, 184 tests). ★검증 중 B5 잠복 크래시(양방향 sync 무한 핑퐁 → Maximum update depth) 발견·수정(발행을 effect→핸들러 이관, 단방향화).
     - 3-D 장소·예산·통계(지표 카드 스택, 축소형) — ✅ 완료(`fbd5fec`): 장소=PlacesMobileControls(폴더 드롭다운 + [리스트|지도] 세그먼트) 단일 트리 CSS 분기(3-B 패턴, 컴포넌트 중복 마운트 없음), 예산/통계=지표 2열·차트 세로 스택·지출 표 가로 스크롤. 375 오버플로 없음·데스크톱 3분할 회귀 없음, 187 tests·build 그린.
  4. **팜플렛/기타** — ⬜
- **관련**: 각 view + `app/layout`, Tailwind 반응형 유틸, `@theme`
- **규모**: L (분할)

---

## A. 버그 (기능 오작동)

### B1. 초대 링크 "복사" 버튼이 클립보드에 복사 안 됨
- **현상**: Step3(멤버 초대)·공유의 "복사" 눌러도 클립보드에 안 들어감.
- **원하는 것**: 클릭 시 실제 복사 + "복사됨" 피드백(토스트/체크).
- **추정 원인**: `src/lib/clipboard.ts` — `navigator.clipboard`는 보안 컨텍스트(HTTPS)+사용자 제스처 필요. 실패 폴백(`execCommand`/textarea) 없거나 async 에러 삼켜짐. 성공/실패 피드백 확인.
- **관련**: `src/lib/clipboard.ts`, `Step3Members.tsx`, `ShareOverlay.tsx`, `SharePublicBar.tsx`
- **규모**: S

### B2. 생성 시 내가 고른 나라/도시가 아니라 "도쿄"로 저장됨
- **현상**: 나라/지역을 입력/선택해도 여행이 도쿄로 저장됨.
- **원하는 것**: 선택한 나라·지역/도시가 그대로 저장·표시.
- **추정 원인**: (1) 나라/지역이 placeholder만 있고 값이 폼에 안 담김(register 누락 가능) → 빈 값이면 어딘가 "도쿄"로 폴백. (2) `create_trip` RPC/`useCreateTrip`가 country/region 무시. **F2(select화)와 함께 해결 권장.**
- **관련**: `Step1Info.tsx`, `CreateTripWizard.tsx`, `useCreateTrip.ts`, `0002_data.sql`(create_trip)
- **규모**: S~M

### B3. 여행 날짜(기간)를 생성 후 수정할 수 없음
- **현상**: 생성 뒤 시작일/종료일 변경 UI 없음.
- **원하는 것**: 여행 설정/상단에서 기간 편집 → 저장.
- **관련**: `WorkspaceTopBar.tsx`(진입점), 신규 `useUpdateTrip`, trip 설정 오버레이
- **규모**: M

### B4. "목록" 버튼이 여행 목록으로 이동 안 됨
- **현상**: 워크스페이스 상단 "← 목록" 눌러도 `/trips`로 안 넘어감.
- **원하는 것**: 클릭 시 여행 목록으로 이동.
- **추정 원인**: 버튼에 onClick/Link 미연결(죽은 버튼) 또는 라우팅 경로 오류.
- **관련**: `WorkspaceTopBar.tsx`
- **규모**: S

### B6. 플랜 Day별 "장소 추가"가 그 날짜에 배정 안 됨
- **현상**: 플랜의 "Day N 일정" 섹션에서 `+ 장소 추가`로 장소를 넣어도, 해당 Day에 안 들어가고 **장소 탭에 미배정 저장만** 됨. 헷갈림.
- **원하는 것**: Day 맥락에서 추가하면 **그 Day에 자동 배정**(scheduled_day=N, 순서 append) + 지도 동선에도 반영.
- **추정 원인**: Day별 "장소 추가" 버튼이 일반 추가 오버레이만 열고 **타깃 Day를 안 넘김** → 저장 시 미배정.
- **해결 방향**: 오버레이 프리필/컨텍스트에 targetDay 전달 → 저장 시 `useAddPlaceToSchedule`(또는 배정)로 그 Day에 넣기. *일반 "장소 추가"(장소 탭)는 미배정 유지, Day 맥락 추가만 배정.*
- **관련**: `PlanView.tsx`/`DayTimeline.tsx`(Day별 추가 버튼), `PlaceDetailOverlay.tsx`, `useAddPlaceToSchedule.ts`, `overlayStore.ts`
- **규모**: M

### B5. 플랜 ↔ 일정표 선택 날짜 미동기화
- **현상**: 플랜에서 4/11을 보다 일정표 탭으로 가면 4/11이 아닌 다른 날짜를 보여줌.
- **원하는 것**: 두 뷰가 **선택 날짜(활성 Day)를 공유** — 플랜에서 보던 날짜가 일정표에도 유지.
- **추정 원인**: `planStore`와 `calendarStore`가 선택 날짜를 각각 따로 관리 → 뷰 전환 시 동기화 안 됨.
- **해결 방향**: 선택 날짜(활성 Day)를 단일 소스로 통합하거나 두 스토어 간 동기화. *플랜/일정표 분리 자체는 의도(공간 렌즈 vs 시간 렌즈)지만 선택 상태는 공유돼야 함.*
- **관련**: `planStore.ts`, `calendarStore.ts`, `PlanView.tsx`, `CalendarView.tsx`, `ViewSegment.tsx`
- **규모**: M

---

### B7. 마법사 e2e 회귀 (flows·template) — 프리필 제거 여파
- **현상**: `e2e/flows`(:48)·`e2e/template`(×2)가 실패. 마법사 Step1 제목이 **미프리필**(G 완료 `2b5567b`)이라, 테스트가 제목을 안 넣고 진행 → 필수 검증에 막혀 Step2로 못 넘어감. (baseline stash에서도 동일 실패 확인 — 3-D와 무관.)
- **원하는 것**: 해당 e2e 스펙이 Step1에서 **제목(및 필요 시 나라/지역)을 입력하는 스텝 추가** → 마법사 관통 정상화. (앱 동작은 정상 — 테스트 픽스처만 stale.)
- **관련**: `e2e/flows.spec.ts`, `e2e/template.spec.ts`
- **규모**: S

## B. 생성 마법사 Step1 (여행 정보)

### F1. 필드 순서 재배치
- **원하는 것**: `여행 제목` → **`나라` → `지역/도시`** → `대표 아이콘` → `커버 색`(꾸미기 뒤로 묶기).
- **관련**: `Step1Info.tsx` · **규모**: S

### F2. 나라·지역/도시를 select(콤보박스)로
- **원하는 것**: 검색 가능한 콤보박스(나라 선택 → 도시 옵션). B2 저장 버그 함께 해결.
- **고려**: 나라/도시 데이터 소스, 자유 입력 허용 여부.
- **관련**: `Step1Info.tsx`, 나라/도시 상수, `components/ui` combobox · **규모**: M

### F3. 커버색 커스텀 팔레트
- **현상**: 프리셋 5색 중 맘에 드는 게 없음.
- **원하는 것**: 색 직접 선택(팔레트/피커) + 프리셋 병행.
- **고려(함정)**: 커버는 `covers.ts` 키(gradient) 기반 → 임의 hex 저장 시 `TripCard`의 `COVER[key].gradient`와 충돌(과거 hex로 500 이력). **렌더 경로도 hex 지원하게 같이 손봐야 함.**
- **관련**: `covers.ts`, `Step1Info.tsx`, `TripCard.tsx`, cover 스키마 · **규모**: M

---

## C. 생성 마법사 Step2 (여행 기간)

### D1. 달력 월 이동 버튼 없음
- **원하는 것**: 월 헤더에 `‹ ›` 이전/다음(+ aria-label). · **관련**: `Step2Dates.tsx` · **규모**: S

### D2. 날짜 input 창 직접 선택·입력
- **원하는 것**: 시작/종료 input 눌러서(또는 타이핑) 날짜 선택. · **관련**: `Step2Dates.tsx` · **규모**: S~M

### D3. 시작일/종료일 배경색 통일
- **원하는 것**: 시작·종료 강조 배경색 통일, 사이 범위와 톤 일관. · **관련**: `Step2Dates.tsx`, `@theme` · **규모**: S

---

## E. UX 배치·라벨

### U1. 초대 링크 공유를 멤버 목록 최상위로
- **현상**: "초대 링크로 공유"가 멤버 목록 맨 아래에 있음.
- **원하는 것**: 목록 최상위로 이동(초대가 우선 액션).
- **관련**: `Step3Members.tsx`, `ShareOverlay.tsx`
- **규모**: S

### U2. 두 개의 "전체" 혼란 — 폴더축 vs 카테고리축 IA 정리
- **현상**: 장소 뷰에 필터 축이 **둘**인데 각각 "전체"가 있어 헷갈림.
  - **폴더축**("전체 폴더"): 사용자가 만든 그룹(맛집투어·쇼핑 등) 필터.
  - **카테고리축**("전체" 칩): 장소 고정 종류(식당/카페/명소…) 필터.
  - 둘은 AND로 동시 적용됨(예: 전체 폴더 + 카페). 하지만 "전체"가 두 개 나란히 = 혼란.
- **원하는 것(택1)**:
  1. **라벨 구분(쉬움)**: 카테고리 "전체" → "모든 종류", 폴더 "전체 폴더" → "모든 폴더".
  2. **시각 분리**: 폴더 필터/카테고리 필터를 구역·구분선으로 명확히 나눔.
  3. **IA 재검토(큰 결정)**: 폴더·카테고리가 겹친다면 하나로 합치거나 한 축 제거.
- **관련**: `FolderSidebar.tsx`, `CategoryBar.tsx`, `PlacesView.tsx`(ALL_FOLDER)
- **규모**: S(라벨) ~ M(IA 재검토)

### U4. "일정표" 탭 → "캘린더"로 리네이밍
- **현상**: "플랜"과 "일정표"가 단어로 잘 구분 안 됨.
- **원하는 것**: `일정표` → **`캘린더`**. "플랜=동선/지도, 캘린더=달력/시간" 대비 명확화.
- **관련**: `ViewSegment.tsx`(탭 라벨), 관련 문구·라우팅 표기(`?view=calendar`는 유지 가능)
- **규모**: S

### U3. 카테고리 필터 칩 가로 스크롤 → 드롭다운
- **현상**: 카테고리 필터(전체·식당·카페·기념…)가 가로로 넘쳐 스크롤해야 함. 특히 모바일에서 불편.
- **원하는 것**: 가로 스크롤 칩 대신 **드롭다운(select)** 으로. 좁은 폭에서 한 번에 선택 가능.
- **고려**: 데스크톱 넓은 폭에선 칩 유지하고 좁을 때만 드롭다운으로 전환하는 반응형도 옵션. (반응형 작업과 연계 가능)
- **관련**: `CategoryBar.tsx` 또는 `PlacesView.tsx`의 카테고리 필터, `components/ui`
- **규모**: S~M

---

### U5. 플랜 CategoryBar "전체 폴더" 버튼 vestigial
- **현상**: 플랜 뷰 일정 패널의 CategoryBar에 있는 "전체 폴더" 버튼이 **onClick 없는 죽은 버튼**(U2 작업 중 발견). 장소 탭 FolderSidebar와 별개.
- **원하는 것(택1)**: (a) 플랜 뷰에서 폴더 필터가 필요하면 배선, (b) 불필요하면 버튼 제거.
- **관련**: `CategoryBar.tsx`
- **규모**: S

---

## 착수 로그
- 2026-07-13: 문서 생성(피드백 11건 트리아지). **R1(반응형) 착수 시작.**
- 2026-07-13: 추가 피드백 — B4(목록 버튼)·B5(플랜↔일정표 날짜 동기화)·U1(초대링크 위치)·U2(전체폴더 라벨) 추가. 설계질문(전체폴더·플랜vs일정표) 답변 완료 — 분리는 의도(공간/시간 렌즈), 선택 날짜는 공유돼야 함(B5).
- 2026-07-13: 순차 진행 — B1·B4(죽은 버튼) → B2·F1·F2(마법사 Step1) → D1·D2·D3(날짜) → R1-2(마법사 반응형) → U4·U1 → B5·B6(플랜/캘린더 동작) → U2·U3·U5(장소 필터) → B3(날짜 수정) → F3(커스텀 커버색) 전부 완료. **남은 것: 워크스페이스 반응형(R1-3, 플랜/캘린더 모바일 시안 대기) · 팜플렛 반응형(R1-4).**
- 2026-07-13: 데모 계정 시딩(scripts/seed-demo.mjs) — demo@jero.travel, 도쿄 여행 샘플 + 읽기전용 공유링크. README 체험 섹션 추가.
- 2026-07-15: R1-3-D(장소·예산·통계 모바일) 완료. ★장소 최초 구현이 데스크톱/모바일 트리를 CSS 로만 이중 렌더 → PlaceList·TripMap 이중 마운트 + folder/place 텍스트 DOM 중복(e2e strict-mode 위반)이 있어 3-B PlanView 패턴(단일 트리 + `md:`/모드 CSS 분기)으로 재작성. folder.spec 폴더명 셀렉터를 데스크톱 사이드바 버튼으로 특정. ※마법사 e2e(flows:48·template ×2)는 step1 제목 미프리필로 **기존 실패**(3-D 무관, stash 검증) — R1 별개 후속.
