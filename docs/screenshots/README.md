# 스크린샷

루트 `README.md` 가 참조하는 이미지들을 여기에 둔다. 파일명을 아래와 같이 맞추면 자동으로 렌더된다.
데스크톱 **1280px** 기준 캡처 권장(시안 기준 폭).

| 파일명 | 내용 |
|---|---|
| `plan-hero.png` | 플랜 뷰 — 좌측 일정 리스트 + 우측 도쿄 지도(동선 Polyline + 번호 마커). hero 이미지. |
| `budget.png` | 예산 대시보드 — 카테고리 도넛 + 일별 추이 막대 + 지출 테이블/정산. |
| `calendar.png` | 일정표 — 월/주/일 모드 중 하나. |

> PNG 권장(폭 ~2560px, 2x). 용량이 크면 최적화 후 커밋.
