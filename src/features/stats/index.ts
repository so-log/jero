/**
 * features/stats — 여행 통계(2차 E). usePlacesQuery 단일 소스를 computeTripStats 로 투영(§7.1).
 */
export { StatsView } from "./components/StatsView";
export { computeTripStats } from "./lib/stats";
export type { TripStats } from "./lib/stats";
